import { TitlecaseCustomPipe } from './titlecase-custom.pipe';

describe('TitlecaseCustomPipe', () => {
  it('create an instance', () => {
    const pipe = new TitlecaseCustomPipe();
    expect(pipe).toBeTruthy();
  });
});
